# SC2 Replay Data Extraction Requirements

# Core dependencies
sc2reader==1.8.0
pandas>=2.0.0
pyarrow>=10.0.0

# Optional (for better performance)
numpy>=1.24.0

# Development (optional)
pytest>=7.3.0
pytest-cov>=4.1.0
